import { PropsWithChildren } from "react";
import { View } from "react-native";

interface AccordionProps {}

export const Accordion = ({ children }: PropsWithChildren<AccordionProps>) => {
  return <View></View>;
};
