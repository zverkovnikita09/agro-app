import { ReactNode } from "react";
import { View } from "react-native";

interface InputFileProps {
  children?: () => ReactNode;
}

export const InputFile = ({ children }: InputFileProps) => {
  return <View></View>;
};
